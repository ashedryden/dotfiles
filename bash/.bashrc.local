source ~/.bashrc.alias
export GIT_AUTHOR_NAME="Ashe Dryden"
export GIT_AUTHOR_EMAIL="ashleydryden@gmail.com"
export GIT_COMMITTER_NAME="Ashe Dryden"
export GIT_COMMITTER_EMAIL="ashleydryden@gmail.com"
if [ -f `brew --prefix`/etc/bash_completion ]; then
  . `brew --prefix`/etc/bash_completion
fi

